// public/index.js

document.addEventListener("DOMContentLoaded", () => {
  // Este código se ejecutará cuando la página esté completamente cargada

  // Agregar un evento de clic a un botón utilizando el ID "myButton"
  const button = document.getElementById("myButton");
  button.addEventListener("click", () => {
    // Aquí puedes agregar lógica para manejar el evento de clic del botón
    alert("¡El botón se hizo clic!");
  });

  // Otros eventos y lógica aquí
});
